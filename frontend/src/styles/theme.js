import { defaultTheme } from './global/theme/defaultTheme'

const theme = {
  ...defaultTheme,
  colors: {
    background: '#13161b',
    text: '#f2f2f2',
    primary: '#455af7'
  }
}

export default theme